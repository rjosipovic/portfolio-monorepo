{{- if .Values.infrastructure.postgres.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
data:
  POSTGRES_USER: {{ .Values.infrastructure.postgres.user | b64enc | quote }}
  POSTGRES_PASSWORD: {{ .Values.infrastructure.postgres.password | b64enc | quote }}
{{- end }}
