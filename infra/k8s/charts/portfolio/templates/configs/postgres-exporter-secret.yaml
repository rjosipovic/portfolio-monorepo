{{- if .Values.observability.postgresExporter.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: postgres-exporter-secret
type: Opaque
stringData:
  DATA_SOURCE_NAME: "postgresql://{{ .Values.infrastructure.postgres.user }}:{{ .Values.infrastructure.postgres.password }}@{{ .Values.infrastructure.postgres.host }}:{{ .Values.infrastructure.postgres.port }}/postgres?sslmode=disable"
{{- end }}